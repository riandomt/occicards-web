<form method="post" action="{{ path('app_folder_delete', { id: child.id }) }}" 
onsubmit="return confirm('Voulez-vous vraiment supprimer ce dossier ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ child.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
